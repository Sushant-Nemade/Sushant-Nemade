# Option 2 Inputs Required

Option 2 is complete with the verified public information currently available. The following optional inputs would unlock richer sections later; none were invented.

- Five to eight self-ratings if a proficiency radar is desired. Each value should be supplied by Sushant and be defensible in an interview.
- Public or sanitized repository URLs for the four case studies when they are ready.
- A portfolio URL, if one exists and should be public.
- A public contact email, only if email should appear on the profile.
- Additional public repositories before a multi-axis repository-language radar becomes meaningful.

Current fallback behavior:

- The radar is labeled **Profile emphasis**, calculated from the number of configured capabilities in each group, and explicitly states that it is not a proficiency score.
- Public language evidence is shown as a proportional bar rather than a misleading one-axis radar.
- Project cards are non-clickable case-study summaries until verified repository URLs are configured.
